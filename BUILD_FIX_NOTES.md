# Fix appliqué

Erreur corrigée :

`The method 'CardThemeData' isn't defined`

Remplacement fait :

`CardThemeData(...)` → `CardTheme(...)`

Compatible avec Flutter 3.24.5 utilisé dans GitHub Actions.
