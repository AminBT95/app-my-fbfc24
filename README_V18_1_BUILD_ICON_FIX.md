# V18.1 Build + Icon Fix

- Fix build: `TacticalPage` converted from `StatefulWidget` to `StatelessWidget`.
- Added launcher icon generation in GitHub Actions after `flutter create`.
- Keeps Android INTERNET permission for minifaces.
